<h1><%= @appointment.doctor.name %></h1>
 
<h3>Category: <%= link_to @appointment.doctor.name, category_path(@appointment.doctor) if @appointment.doctor %></h3>
 
<p><%= @appointment.patient.name %></p>

<h3>Category: <%= link_to @appointment.patient.name, category_path(@appointment.patient) if @appointment.patient %></h3>
